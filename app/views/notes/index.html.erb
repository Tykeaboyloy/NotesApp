<ul class="ml-10 mt-5" style="list-style-type:disc ;font-size: 1.5rem">
  <% @notes.each do |note| %>
    <li>
      <%= link_to note.title, note %>
    </li>
  <%end%>
</ul>
<div class="ml-15 mt-5" style="display:inline-block;margin-left:30px;color:teal">
  <%= link_to 'Add Note', new_note_path %>
</div>
